// make an endpoint to verify email
const sendEmail = require('../novu/send-verification-email')
const nodemailer = require("nodemailer");

const sendVerificationEmail = async (req, res) => {
    
}

module.exports = sendEmail;

